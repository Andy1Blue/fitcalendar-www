import * as React from 'react';
import { useEffect } from 'react';
import './CalendarTiles.scss';

interface DayModalProps {
  training: any;
}

const DayModal = ({  }: DayModalProps) => {


  useEffect(() => {
   
  }, []);

  return (
    <div className="dayModal">
      test
    </div>
  );
};
export default DayModal;
